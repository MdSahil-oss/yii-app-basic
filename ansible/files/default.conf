server {
    listen 80 default_server;
    server_name _;  # Optional: Explicitly set an invalid server name

    location / {
        proxy_pass http://localhost:8080; # Or your backend server
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}